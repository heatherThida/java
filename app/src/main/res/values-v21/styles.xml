<resources>
    <!--TODO(kcampbell):-->
    <!--1. Make sure new colors (taken from StreetConnect Logo Palette) match up all over app. -->

    <!-- values-v21 here. This'll let me experiment with material design while keeping the app basic for holo users. -->
    <!-- this application (at least in testing, only on api 21, uses the style "Test" -->

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/MyActionBar1</item>
    </style>

    <style name="MyActionBar1" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">#C1000E</item>
        <item name="android:titleTextStyle">@style/AppTheme.ActionBar.TitleTextStyle</item>
    </style>

    <style name="AppTheme.ActionBar.TitleTextStyle" parent="@android:style/TextAppearance.StatusBar.Title">
        <item name="android:textColor">#E5ED0E</item>
    </style>


    <style name="Test" parent="Theme.AppCompat.Light">
         <!-- Main theme colors -->
         <!--   your app branding color for the app bar -->
         <item name="android:colorPrimary">#DD6721</item>
         <!--   darker variant for the status bar and contextual app bars -->
         <item name="android:colorPrimaryDark">#DA4800</item>
         <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">#B6B6B6</item>
        <item name="android:textColor">#212121</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>

        <!-- color for the app drawer highlight. actual color stored in colors.xml-->
        <item name="android:activatedBackgroundIndicator">@drawable/activatedbackgroundindicator</item>

        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="android:actionMenuTextColor">#FFFFFF</item>

        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="actionMenuTextColor">#FFFFFF</item>
        <item name="colorAccent">@color/streetconnectOrange</item>

    </style>


    <!-- general styles for the action bar -->
        <style name="MyActionBar"
            parent="@style/Widget.AppCompat.ActionBar">
            <item name="android:titleTextStyle">@style/TitleTextStyle</item>
            <item name="android:background">@drawable/darkprimary</item>
            <!--<item name="android:backgroundStacked">@drawable/actionbar_background</item>-->
            <!--<item name="android:backgroundSplit">@drawable/actionbar_background</item>-->

            <!-- Support library compatibility -->
            <item name="titleTextStyle">@style/TitleTextStyle</item>
            <item name="background">@drawable/darkprimary</item>
            <!--<item name="backgroundStacked">@drawable/actionbar_background</item>-->
            <!--<item name="backgroundSplit">@drawable/actionbar_background</item>-->
        </style>

        <!-- action bar title text -->
        <style name="TitleTextStyle"
            parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
            <item name="android:textColor">#B6B6B6</item>
        </style>

        <!-- action bar tab text -->
        <style name="TabTextStyle"
            parent="@style/Widget.AppCompat.ActionBar.TabText">
            <item name="android:textColor">#B6B6B6</item>
        </style>


        <!--light primary color: #FFCCBC
            divider color: #B6B6B6-->
</resources>
